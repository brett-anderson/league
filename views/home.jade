extends layout

block content
  div.container
    div.row
      div.span12
        h1.text-center Welcome #{user.firstName}. Check your details below:
        div
          ul.user-details
            li Username ---> #{user.username}
            li Email    ---> #{user.email}
            li First Name ---> #{user.firstName} 
            li Last Name ---> #{user.lastName}
    div.row-fluid
      //- div.col-md-3.col-centered.login
      //-   form(class='form-signin', action='/api/bet', method='POST')
      //-         input.form-control(type='text', name='title' placeholder='title', required, autofocus)
      //-         input.form-control(type='num', name='amount' placeholder='amount', required, autofocus)

      //-         button(class='btn btn-lg btn-primary btn-block', type='submit') Create Bet
      //-     #message
      //-     if message
      //-       h1.text-center.error-message #{message}
      div#bets
    a(href='/signout', class='text-center new-account') Sign Out
